// 1. NUMBER TYPE
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String Type
let myName = "LEE";
let myLocation = "BC";
let introduce = myName + myLocation; //LEEBC

let introduceText = `${myName} lives in ${myLocation}`;
console.log(introduceText);
console.log(introduceText);

// 3. Boolean TYpe
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type(Nothing)
let empty = null;

// 5. Undefined Type
let none;
console.log(none); //undefined : 초기화 못했거나 존재하지 않은 값을 불러오려고 할 때